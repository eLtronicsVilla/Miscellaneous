#include <stdio.h>
//#include <strlib.h>


int main()
{
char ch[10] = "brijesh";
int len;

len = my_len(ch);
printf("string length is = %d\n",len);
}

int my_len(char my_ch)
{
for (int i=0;my_ch[];i++)
len++;
return len;
}
